"""
User-defined external plugins.

Drop .py files here — ToolRegistry auto-discovers them on startup.
Each module should define tool classes that subclass BaseTool.
See plugin.md for the full guide.
"""
